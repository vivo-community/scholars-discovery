PREFIX vitro: <http://vitro.mannlib.cornell.edu/ns/vitro/0.7#>
PREFIX tamu: <http://vivo.library.tamu.edu/ontology/TAMU#>

CONSTRUCT {
    <{{uri}}> tamu:fullAuthorList ?featuredProfileDisplay .
} WHERE {
    <{{uri}}> tamu:fullAuthorList ?featuredProfileDisplay .
}