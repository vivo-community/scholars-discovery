PREFIX tamu: <http://vivo.library.tamu.edu/ontology/TAMU#>

CONSTRUCT {
    <{{uri}}> tamu:FeaturedProfileDisplay ?featuredProfileDisplay .
} WHERE {
    <{{uri}}> tamu:FeaturedProfileDisplay ?featuredProfileDisplay .
}