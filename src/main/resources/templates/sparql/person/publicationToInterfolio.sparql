PREFIX tamu: <http://vivo.library.tamu.edu/ontology/TAMU#>

CONSTRUCT {
    <{{uri}}> tamu:PublicationToInterfolio ?publicationToInterfolio .
} WHERE {
    <{{uri}}> tamu:PublicationToInterfolio ?publicationToInterfolio .
}
